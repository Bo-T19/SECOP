{
  "name": "secop",
  "version": "1.0.0",
  "description": "A little backend that fetches data from SECOP in order to analyse it and send it to DCD's commercial team",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "SECOP",
    "DCD",
    "IA"
  ],
  "author": "DCD",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.9.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
